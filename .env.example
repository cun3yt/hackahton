# Local secrets — gitignored. Fill in values; never commit.

# Claude model key used by CopilotKit BuiltInAgent (console.anthropic.com → API keys)
ANTHROPIC_API_KEY=

# Ambiguous API key (ak_…). Filled automatically from ./.ambi/config.json after `auth login`.
AMBI_API_TOKEN=
AMBI_API_URL=https://app.ambiguous.ai
